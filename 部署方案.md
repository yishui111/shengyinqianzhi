# 部署方案（E:\shengyinqianzhi 声音素材前置处理）

> 换电脑复原依据；**每次优化/修复后必须同步更新本文件**。

## 1. 项目简介

独立的声音素材前置处理项目（端口 8070）：把各种音频（wav/mp3/m4a/flac/aac/ogg）和视频（mp4/mkv/mov/avi/flv）
处理成适合训练模型的干净人声：
- 从视频中自动提取音轨（ffmpeg）
- pymss 人声分离去背景音乐/伴奏（bs_roformer_voc_hyperacev2）
- 可选去混响（dereverb_mel_band_roformer）
- 可选"只保留主要说话人"（ECAPA 声纹聚类，去掉视频里其他人的声音，默认开）
- 默认输出**完整音频**（方便试听）；"生成训练切片"为可选项
- 响度归一化(-16 LUFS)
- 输出 44.1k（可选 32k/16k）单声道 wav

## 2. 新电脑要求

**本项目完全自包含**：运行时、ffmpeg、全部模型都已放进项目文件夹，
把 `E:\shengyinqianzhi` 整个文件夹复制到任意电脑即可直接运行（无需安装 Python/依赖/联网）。
- `runtime\py312\`：便携 Python 运行时（含 torch/pymss/speechbrain/fastapi 等全部依赖，约 9.4GB）
- `ffmpeg\`：ffmpeg/ffprobe（约 0.2GB）
- `models\pymss\`：人声分离 + 去混响模型（约 1.1GB）
- `models\speaker\` + `models\hf_cache\`：ECAPA 说话人识别模型（约 0.2GB）
- `models\asr\`：语音转文字模型 SenseVoiceSmall + FSMN VAD（约 0.9GB，2026-08-22 新增）
- NVIDIA 显卡（人声分离建议 ≥6GB 显存；没有 GPU 自动走 CPU 但慢）

## 3. 目录结构

```
E:\shengyinqianzhi\
├─ pre_service\preprocess_api.py   主服务（FastAPI，8070）
├─ runtime\py312\                  便携 Python 运行时（自包含）
├─ ffmpeg\                         便携 ffmpeg
├─ models\                         全部模型（pymss / speaker / hf_cache / asr）
├─ ziliao\input\                    网页上传的原始素材（按任务时间分目录）
├─ ziliao\output\<任务时间>\        处理结果 wav（+ zip 打包下载）
├─ ziliao\output\merged\            「合成一个音频」的合并结果 wav
├─ ziliao\asr\<时间戳>\             语音转文字结果 txt 文档
├─ tests\                           测试文件夹
├─ 一键启动素材前置处理.bat / 关闭素材前置处理.bat
├─ 部署方案.md / AGENTS.md
```

## 4. 启动/停止

- 启动：双击 `一键启动素材前置处理.bat` → http://127.0.0.1:8070/
- 停止：双击 `关闭素材前置处理.bat`（按端口 8070 杀进程）
- 自检：`GET /api/health`

## 4.1 换电脑部署（一键复原）

1. 把 `E:\shengyinqianzhi` 整个文件夹复制到新电脑任意位置
2. 双击 `一键启动素材前置处理.bat`（脚本全部用相对路径，不依赖 E:\tihuanshengyin）
3. 打开 http://127.0.0.1:8070/ 即可使用
4. 若新电脑没有独立显卡，代码自动走 CPU（人声分离会慢一些，但可用）

## 5. 使用流程

1. 网页选文件（可多选音频/视频）上传，或填服务器文件夹路径批量读取
2. 处理选项**默认全部不勾选**：什么都不勾 = 只把视频/音频原样转成 44.1k 单声道 wav（纯转码，适合方言录音/干净素材）
   需要去 BGM 时勾"人声分离"；去混响勾"去混响"；去其他说话人勾"只保留主要说话人"；去掌声勾"去掌声/欢呼/音效"；
   响度归一化按需勾选；"生成训练切片"默认不勾——默认输出完整 wav 供试听；需要训练片段时再勾选
3. 点开始，日志实时显示；完成后结果列表**可直接页面试听、单个 wav 直接下载**（zip 仅作批量下载备选）
4. 试听确认没问题后，把结果 wav 放进训练中心 `E:\xunlianzhongxin\ziliao\xunlianshuju\<角色名>\`（或训练中心直接填单个文件路径），训练中心会自动切分训练
5. **合成一个音频**：页面底部"合成一个音频"卡片 → 选择合并范围（全部输出 / 指定任务）→ 点"合成并下载"，把该范围内所有结果 wav 按文件名顺序拼成一个 44.1k 单声道 wav 直接下载（自动跳过中间文件 work 目录）

## 6. 接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/` | GET | 网页界面 |
| `/api/process` | POST | multipart 上传文件处理（files + opts JSON） |
| `/api/process_dir` | POST | 服务器文件夹批量处理（dir + 选项） |
| `/api/status` | GET | 任务状态/日志/结果 |
| `/api/result/<任务id>/zip` | GET | 下载全部结果 zip |
| `/api/result/<任务id>/file/<文件名>` | GET | 下载/试听单个结果 wav |
| `/api/tasks` | GET | 列出有结果的任务目录（供合并选择） |
| `/api/merge` | POST | 合成一个音频：body `{"task": "任务id"}` 或 `{"task": "all"}`（全部），返回合并后的 wav |
| `/api/asr` | POST | 语音转文字（multipart 上传文件） |
| `/api/asr_path` | POST | 语音转文字（body `{"path": "服务器文件路径"}`） |
| `/api/asr_file/<时间戳>/<名>.txt` | GET | 下载语音转文字生成的 txt 文档 |
| `/api/health` | GET | 健康检查 |

## 7. 已知问题 / 注意事项

- 一次只跑一个任务（防止模型状态互相干扰）；任务排队等待。
- 长音频自动按 90 秒分段分离再拼接，防爆显存。
- 人声分离会把干净录音的音高略微改变（引擎特性）：干净独白建议直接关闭"人声分离"，只开切片清洗；带 BGM 的网上下载素材必须开。
- "只保留主要说话人"按说话时间最长的那个保留：视频里谁说话最久就留谁；如果想留的不是主角，需先剪辑素材再处理。
- ECAPA 声纹模型首次使用自动下载（约 50MB，存 `E:\tihuanshengyin\runtime\cache`）。
- 输出 wav 是给训练用的：RVC 用 44.1k；GPT-SoVITS 训练时会自行重采样，44.1k 即可。
- "合成一个音频"（/api/merge）是 CPU + 磁盘操作，不与模型任务冲突；合并范围"全部"会包含所有历史任务（含早期测试输出），通常选**最近一次任务**即可。

## 8. 功能变更记录

- 2026-08-20：新增「合成一个音频」：后端 `/api/merge` + `/api/tasks`，前端页面底部新增合并卡片；
  合并逻辑：先统一转成 44.1k 单声道 pcm_s16le 再 concat 无损拼接，输出到 `ziliao\output\merged\`。
  自测记录：leijun 任务 `20260819_235149`（62 段）合并成功，输出 437.99 秒 = 各段时长之和（差值 0），
  格式 pcm_s16le/44100/单声道；正式产物 `merged_leijun_20260811_20260820_000729.wav`（36.84 MB）。
- 2026-08-20（修正）：合并范围默认改为**最近一次任务**（而不是"全部输出"），"全部输出"移到最后并标注"慎用"；
  明确合并语义 = 单次任务（一个队列）的结果 wav 按顺序拼接，防止误把全部历史任务合在一起。
- 2026-08-20：新增独立选项「去掌声/欢呼/音效」（denoise）——把原来绑在"只保留主要说话人"里的
  `remove_unvoiced_regions`（基频检测，剪掉连续无基频 >0.7s 的长片段：掌声/欢呼/长停顿/无基频音效）拆成独立勾选项，默认开。
  自测记录：`tests\denoise_test\denoise_src.wav`（5s 正弦波=语音 + 2s 白噪声=掌声，共 7s）
  开启 denoise → 输出 4.9s（剪掉掌声段，日志"去非人声：剪掉 1 段"）；关闭 → 输出 7.0s（原样保留）。
- 2026-08-20：**处理选项默认全部不勾选**——什么都不勾 = 只把视频/音频原样转成 44.1k 单声道 wav（纯转码，不做任何 AI 处理），
  适合方言录音/干净素材（方言基频模式特殊，人声分离/说话人过滤/去掌声可能误伤）。
  自测记录：`tests\denoise_test\video_src.mp4`（7s 视频+音频）全不勾处理 → 输出 7.0s wav，日志"未勾选任何处理项：仅提取音频"。
- 2026-08-22：新增「提高人声音量」（boost）——勾选后按增益倍数放大音量（默认 2 倍，可调 1~20 倍，防削波 clip），
  人声偏小/远距离录音适用；可单独使用（不勾其他处理，只提音量）。
  自测记录：`tests\boost_test\quiet_src.wav`（-30dB 正弦，RMS 0.00279）boost 4 倍 → 输出 RMS 0.01117（正好 4 倍），日志"提高音量：4.0 倍"。
- 2026-08-22：新增「语音转文字」（ASR，funasr + SenseVoiceSmall）——页面「📝 语音转文字」卡片：
  上传音频/视频（或填服务器路径）→ 识别成中文文本（支持方言，输出简体中文）→ 页面展示/复制全文/下载 txt 文档。
  模型 SenseVoiceSmall（~900MB）+ FSMN VAD（~4MB）从 modelscope 下载到 `models\asr\`（一次性，离线可用）。
  接口：`POST /api/asr`（上传）、`POST /api/asr_path`（路径）、`GET /api/asr_file/<时间戳>/<名>.txt`（下载文档）；
  结果 txt 存 `ziliao\asr\<时间戳>\`。
  自测记录：识别 `ziliao\output\20260819_235149\leijun_20260811_00_clean.wav`（9.6s）→
  "世界发生了巨大的变化，已经很深的影响到了我们每一个人的生活和工作。面对连续巨大的变化，不少人觉得不适应。"，
  首次含模型加载共 9.5s，识别 rtf≈0.02（GPU 上约 50 倍实时）。
- 2026-09-05：**修复「去静音/切片」在真实语音上 0 输出的 bug**——原 `voice_segments` 在当前段时长超过
  silence_gap 后，遇到**第一个**低于能量阈值的帧（10ms）就切分，并非"静音持续 0.45s 才分段"；
  自然语音的换气/爆破音短促停顿把语音切碎成 <1.5s 碎片，再被最短时长过滤全部丢弃（切片 = 0 个输出）。
  改为：静音**连续持续**超 silence_gap 才分段。自测记录：`tests\test_speech.wav`（12s 真实语音）
  修复前 0 段 → 修复后 2 段（8.07s + 2.50s）；`tests\test_long108.wav`（100s）→ 17 段；
  合成"连续语音"（每 0.3s 嵌 60ms 微停顿）6s → 1 段（不再被切碎）。
- 2026-09-05：「只保留主要说话人」改为**按说话总时长最长的声纹簇保留**（与页面/文档"按说话时间最长者保留"
  的承诺一致；原实现取"与所有段最相似的居中段"+邻域阈值，保留结果不稳定）。分段质量受益于上一条修复。
  自测记录：test_speech（24s，两份）+ 冯彦林（598.5s）拼接 624.9s → 切出 141 段，保留主簇 139 段
  （输出 404.5s，即冯彦林全部语音段，段间停顿被拼接压缩；声纹相似度实测：冯彦林簇内 0.682，跨组均值 0.382）。
- 2026-09-05：「去掌声/欢呼/音效」两处加固：① 基频分析改在 16k 降采样拷贝上进行（长视频省近 1GB 内存、提速，
  剪切仍按原采样率在原音频上执行）；② 剪完剩 <1.5s 判为误判、保持原样（防止整段无人声素材输出空文件）。
  **生效前提（已在网页选项说明中写明）**：素材自带背景音乐时音乐带基频、剪不动，必须同时勾「人声分离」；
  与人声重叠的掌声也剪不到。自测记录：`tests\test_mixed_bgm.wav` 分离+去掌声：12.0s → 7.1s（剪 3 段）；
  `tests\denoise_test\denoise_src.wav`：7.0s → 4.9s（剪 1 段，与 2026-08-20 记录一致）。
- 2026-09-05：网页「处理选项」说明改为如实描述各功能生效前提，并新增"常用组合"提示行；
  新增回归脚本 `tests\diag_features.py`（单元级：基频判断/去掌声/切分/说话人）与
  `tests\diag_e2e.py`（GPU 端到端：切片/人声分离/分离+去掌声），均已跑通。
- 2026-09-05：**页面精简，突出核心干音流程**——「去掌声/欢呼/音效」「只保留主要说话人」两个进阶选项
  从页面移除（实测对带 BGM 的常规素材效果有限/依赖素材条件），核心流程加粗突出：
  ★人声分离、去混响、提高人声音量、训练切片、响度归一化、输出采样率。
  后端 API 完整保留这两项能力：提交 opts 时带 `"speaker": true` / `"denoise": true` 即可使用
  （POST /api/process 的 opts 字段或 /api/process_dir 的 JSON body）。
  自测记录（浏览器实测页面↔后台联动）：页面勾「切片+归一化」+ 服务器路径 `ziliao\input\ui_test`
  → 任务 `20260905_024301` → 日志"切片：2 段"、结果区表格 2 行与 /api/status results 一致
  （test_speech_p00.wav 8.1s / p01.wav 2.5s），磁盘文件 pcm_s16le/44100/单声道核对一致。
- 2026-09-05（二）：页面再移除「额外生成训练切片」——用户工作流里训练中心接收**完整语音**并自行切片，
  前置处理阶段切片多余。页面现仅剩 4 个核心选项：★人声分离、去混响、提高人声音量（+增益）、响度归一化。
  后端 `do_clean` 能力保留：提交 opts 带 `"clean": true` 仍可切片（API 用户/日后回退用）。
  自测记录（浏览器实测）：页面只勾「响度归一化」跑 `ziliao\input\ui_test` → 任务 `20260905_024819`
  输出**单个完整文件** test_speech_clean.wav（12s，非切片），页面结果区与 /api/status 一致。
- 2026-09-05（三）：**本机私有化 + 数据不保留**两项策略落地：
  ① 服务监听地址 `0.0.0.0` → `127.0.0.1`（仅本机可访问，素材不出这台电脑；环境变量 `PRE_HOST` 可覆盖）；
  ② 「结果不保留」：服务**启动时**与**每个新任务开始时**自动清空 `ziliao\input\`（上传原件）、
  `ziliao\output\`（处理结果 + merged 合并音频）、`ziliao\asr\`（转写 txt）；ASR 上传的源件转写完立即删除。
  结果保留到下一次任务开始或服务重启为止，要留的东西从页面下载。处理中若提交新任务仍先拒绝（400），
  清理只发生在确认接单之后，不会误删正在跑的任务数据。
  自测记录：预置假数据 output/input/asr/merged 各一处 → 重启服务 → 全部清空；netstat 确认监听
  `127.0.0.1:8070`；页面连跑任务1（归一化）→ 任务2（纯转码），任务2开始后任务1的输出目录自动消失，
  input/asr 目录为空，页面结果区与 /api/status 一致。
- 2026-09-05（四）：**代码排查优化一轮**：
  ① 长音频分段分离/去混响改为**重叠 0.5 秒交叉淡化拼接**（原为硬拼接，段边界可能有爆音/不连续）；
    相邻段起点差 90s-0.5s，重叠区交叉淡化，总长严格不变；末段不足 2 秒并入前一段。
    自测：恒等分离模拟器 7 种时长情形（50/89.7/90/91/92.5/100/180 秒）全部总长不变、波形逐点一致。
  ② **同名素材不再互相覆盖**：同一批上传同名文件、或同名不同扩展名（如 a.wav+a.mp4）→ 输出自动加序号
    （same_2.wav / dup_clean_2.wav）。自测：页面批 dup.wav+dup.mp4 → dup_clean.wav + dup_clean_2.wav；
    curl 上传两个 same.wav → same.wav + same_2.wav，输出两个独立文件。
  ③ **批处理容错**：单个文件损坏/不支持时跳过继续跑完剩余文件，结束后页面顶部提示
    "⚠ 完成，但 N 个文件失败：xxx"（原为一个坏文件拖死整批）。自测：好文件+坏文件+重名对 →
    3 个输出、页面 ⚠ 提示 broken.wav。
  ④ 上传格式校验：非音视频扩展名直接 400 并列出被跳过的文件（原来到 ffmpeg 才报错）。
  ⑤ 内存/性能：读取音频直接 float32（原 float64 再转，45 分钟视频峰值多占约 500MB）；
    ffmpeg 提取音轨加 `-vn`。
  ⑥ 分段处理进度：日志显示"第 x/N 段（起~止 秒）"。
  ⑦ ASR 文本清洗修正：删除"去开头数字"的正则（实测 funasr/SenseVoice 原始输出从不带数字序号，
    该正则只会误伤以数字开头的正文，如"2026年"）。
